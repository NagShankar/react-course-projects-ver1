"use strict";

// JSX - JavaScript XML
var template = React.createElement("p", null, "He won the first ever thailand grand prix");
var appRoot = document.getElementById('app');
ReactDOM.render(template, appRoot);
